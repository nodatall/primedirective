interface:
  display_name: "Deliver"
  short_description: "Plan, refine, then execute"
  default_prompt: "Use $deliver to create or update one readable execution plan, run --pro-analysis immediately when I include it, refine the plan in the same command, and get approval before execution starts. Use $deliver discuss only when I explicitly want a draft checklist to stay current while we talk through changes."
