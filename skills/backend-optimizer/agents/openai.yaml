interface:
  display_name: "Backend Optimizer"
  short_description: "Optimize backend performance"
  default_prompt: "Use $backend-optimizer to inspect backend and database performance, rank measured candidates, and run the appropriate report-first or /goal optimization loop."
