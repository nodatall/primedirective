interface:
  display_name: "Plan To Goal"
  short_description: "Turn adaptive plans into /goal prompts"
  default_prompt: "Use $plan-to-goal to convert this source plan into a reviewable goal-plan doc with a compact paste-ready /goal prompt, without starting the goal."
